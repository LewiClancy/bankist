import { LinkHoverDirective } from './link-hover.directive';

describe('LinkHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkHoverDirective();
    expect(directive).toBeTruthy();
  });
});
